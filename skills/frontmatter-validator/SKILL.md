---
name: frontmatter-validator
description: Validate YAML frontmatter for a repository's declared documentation schema.
---

# Frontmatter validator

Enumerate in-scope Markdown files, inspect each opening YAML block directly, and report malformed or missing fields. Use the consuming repository's schema; do not invent required fields. Make corrections only when authorized and then rerun the audit.
